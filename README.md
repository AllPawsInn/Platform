# Build Commands

  `npm install --save electron`
  
Dev Run 

  `npm start`


Must Have

  `npm install --save-dev electron-packager`

Publish Version

  `npm run package-mac`
  
  `npm run package-win`
  
  `npm run package-linux`

